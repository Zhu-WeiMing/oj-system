@startuml
start
:管理员登录系统;
if (登录成功?) then (是)
  :进入【题目管理】模块;
  :点击【新建题目】按钮;
  :进入题目创建页面;

  repeat
    :填写题目标题（必填）;
    :输入题目描述（Markdown格式）;
    :设置难度等级（简单/中等/困难）;
    :添加分类标签（多选）;
    :配置判题规则\n（时间限制/内存限制）;
    :编辑测试用例\n（输入输出JSON数组）;
  repeat while (数据校验失败?) is (是)
    :显示具体错误提示\n（如标题超长/测试用例格式错误）;

  :点击【发布】按钮;
  fork
    :持久化到MySQL数据库\n（写入question表）;
  fork again
    :更新Redis题目缓存\n（最新题目列表）;
  fork again
    :同步到Elasticsearch\n（全文检索索引）;
  end fork

  if (所有操作成功?) then (是)
    :跳转至题目中心页面;
    :显示"发布成功"Toast提示;
    :前端自动刷新题目列表;
  else (部分失败)
    :触发分布式事务回滚;
    :显示"发布失败"警告弹窗\n（附错误日志链接）;
  endif
else (否)
  :显示"身份验证失败"提示;
  :跳转至登录页面;
endif
stop
@enduml